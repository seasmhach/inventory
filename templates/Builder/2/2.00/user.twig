<?php
{% include 'license.twig' %}

namespace Builder;
use Builder\Base{{ namespace }};

class {{ namespace }} extends Base{{ namespace }} {
	public function __construct(string $alias = '{{ table_name }}') {
		parent::__construct($alias);
	}
}
